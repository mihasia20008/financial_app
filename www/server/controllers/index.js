const users = require('./users');
const bills = require('./bills');
const billTypes = require('./billTypes');
const operations = require('./operations');
const categories = require('./categories');

module.exports = {
  users,
  bills,
  billTypes,
  operations,
  categories
};
